'''defines the sets and surfaces to be used in the model with descriptive names

each layer must be a set

each part

each contact face with a descriptive name must be a surface

regions with

heavily reliant on routines for selecting geometry

'''

from .routines import util

print(f'this is the {__name__} routine running in abaqus interpreter...')